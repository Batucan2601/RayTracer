#pragma once 
#include "../Dependencies/glm_headers/glm.hpp"
#include "Ray.h"
#include <vector>
#include <memory>

namespace Scene
{
    /*
    static std::vector<std::unique_ptr<Object>> object_list; 
    static void add_sphere(const glm::vec3& center , const float & radius )
    {
        std::unique_ptr<Object> p  
        Scene::object_list.push_back(std::make_unique<Sphere>(center, radius));
    }
    static void save_scene();
    */

};